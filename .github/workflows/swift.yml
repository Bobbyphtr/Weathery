name: Swift

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: xcodebuild -scheme WeatheryApp clean test | xcpretty
#     - name: Test
#       run: cd WeatheryAppTests; swift test --parallel; cd ..
